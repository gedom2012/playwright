import { test } from '@playwright/test';

test.describe('Checkout overview', () => {
  test('should display all products in the order summary', ({ page }) => {});

  test('should display payment information', ({ page }) => {});

  test('should display shipping information', ({ page }) => {});

  test('should display the total amount due', ({ page }) => {});

  test('should allow canceling the checkout', ({ page }) => {});

  test('should successfully complete the checkout', ({ page }) => {});
});
