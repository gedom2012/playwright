import { test } from '@playwright/test';

test.describe('Checkout Information', () => {
  test('should proceed to checkout overview after filling out the form', ({ page }) => {});

  test('should navigate back to the cart from checkout', ({ page }) => {});

  test('should display an error when a required field is missing', ({ page }) => {});
});
